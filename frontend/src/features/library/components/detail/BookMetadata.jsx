/**
 * @fileoverview BookMetadata — small metadata row (difficulty, JLPT, length,
 * availability) reused across BookDetailContent and card variants.
 * Placeholder for Phase 9. No implementation yet.
 */
export default function BookMetadata() {
  return null;
}
