/**
 * @fileoverview DEPRECATED — Phase 10 implemented import progress as
 * button-state changes on BookCard's grid variant (see useImport +
 * getImportButtonProps in BookCard.jsx), not as a separate progress UI.
 * This file is unused and will be removed in Phase 11 cleanup.
 */
export default function ImportProgress() {
  return null;
}
