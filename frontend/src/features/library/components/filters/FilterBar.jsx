/**
 * @fileoverview FilterBar — primary filter row (genre, difficulty, jlpt,
 * length, theme, mood) plus sort and the "More Filters" trigger. Backed by
 * useTaxonomy and useCatalog.
 * Placeholder for Phase 3. No implementation yet.
 */
export default function FilterBar() {
  return null;
}
