# Park Doc — Backend Engineer
**Date**: 2026-06-14 14:00
**Session**: Stripe Block 3 — Webhook Handler
**Engineer**: backend-engineer

---

## What was done

### 6.0 — Debug line cleanup
- Found 3 debug commits: `715d702` (logger.info), `66170df` (changed to print), `19d1484` (second print)
- Removed both resulting print statements from the Stripe config section of server.py
- Diff confirmed: exactly 2 lines removed, nothing else
- Commit: `9bb923d` — pushed to main

### 6.1 + 6.2 + 6.3 — Webhook endpoint, idempotency, event handlers
- Added `Request` to FastAPI imports (required for raw body access)
- Added `POST /api/webhooks/stripe` directly on `app` (not `api_router`) — raw body preserved for Stripe signature verification
- Signature verification: `stripe.Webhook.construct_event(payload, sig_header, STRIPE_WEBHOOK_SECRET)` — `ValueError` or `SignatureVerificationError` → 400
- Idempotency: `stripe_events` collection, deduped by Stripe event ID (`_id: event["id"]`). Record inserted AFTER handler success, not before.
- 4 handler functions: `_handle_checkout_completed`, `_handle_checkout_expired`, `_handle_subscription_updated`, `_handle_subscription_deleted`
- Founding-member spot rollback uses `$expr: {"$lt": ["$spots_remaining", "$total_spots"]}` as upper-bound guard
- Unhandled event types: logged and acknowledged with 200 (Stripe doesn't need a 4xx for events we don't care about)
- Commit: `5a81034` — pushed to main

### 6.4 — Documentation
- `api-contracts.md`: added full webhook contract (event table, idempotency note, error table)
- `db-schema.md`: added `stripe_events` collection; updated `app_config` write contract note

---

## Commits (Block 3)

| Commit | Description |
|---|---|
| `9bb923d` | chore: remove temporary stripe env var debug lines |
| `5a81034` | feat: add stripe webhook endpoint with signature verification, idempotency, and event handlers |
| (docs commit — pending) | docs: update api-contracts and db-schema for Block 3 |

---

## Testing status and gaps

### Confirmed via code review (not yet live-tested):
- Signature verification logic matches Stripe's documented pattern exactly
- Idempotency insert-after-success pattern is correct per brief specification
- `$expr` upper-bound guard on spot rollback is correct MongoDB syntax

### Pending — requires Nico to complete Part A (webhook registration in Stripe dashboard):
1. **Signature verification passes**: send a test event from Stripe dashboard → Railway logs should show no 400
2. **`checkout.session.completed` (most important)**: do a full test checkout → `GET /api/auth/me` should show `subscription_tier: "premium"` or `"founding_member"` with `subscription_status: "active"`
3. **`checkout.session.expired`**: use Stripe dashboard "Send test webhook" with crafted payload (real expiry takes 24h in test mode — not practical to wait)
4. **Subscription cancel/delete**: requires an active subscription from step 2, then cancel via Stripe dashboard Customers view, observe webhook fire

### Known testing gap:
`checkout.session.expired` cannot be easily triggered by a real expiry in test mode (24h wait). Tested via synthetic "Send test webhook" from Stripe dashboard only. Acceptable for launch — the rollback code path is simple (single `update_one`) and the `$expr` guard is the only non-trivial part, which can be verified by MongoDB query after synthetic event.

---

## Design decisions logged

**Idempotency insert order** (insert AFTER success, not before): if handler fails partway, no dedupe record exists, so Stripe's retry will re-run the handler. Risk: double-processing on handler partial-success. Accepted tradeoff per brief — not over-engineering for launch.

**Unhandled events return 200**: Stripe keeps retrying if it gets a non-2xx. For event types we don't handle (e.g. `invoice.paid`), we log and return 200 immediately. This avoids Stripe filling our logs with retried deliveries for events we don't care about.

**`customer.subscription.updated` sets `"canceled"` on `cancel_at_period_end: true`**: user retains `subscription_tier` (still "premium") but `subscription_status` flips to "canceled." Frontend gating (Block 4) must read BOTH fields — `subscription_tier == "premium" AND subscription_status == "canceled"` means "still has access, but won't renew." This is confirmed business logic from Nico.

---

## Open items for COO / Block 4

1. **`STRIPE_WEBHOOK_SECRET` update** (Nico's Part A): must be done before webhook testing is possible. Current Railway value is `placeholder_not_set_yet` — signature verification will fail until this is set to the real `whsec_...` value.
2. **Upgrade path (Premium → Founding Member)**: still blocked by the already-subscribed check in Block 2. COO should decide whether to support this before Block 4 builds the UI.
3. **Free-tier AI cap** (`ai_messages_today`/`ai_messages_date`): fields exist since Block 1, logic not built. COO to scope for Block 4 or Block 5.
4. **`stripe_events` TTL index**: not set. Collection will grow unboundedly. Not a launch blocker, but worth a small maintenance task post-launch.
